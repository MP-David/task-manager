spring:
  datasource:
    url: 'jdbc:mysql://${MYSQL_HOST:localhost:3306/task_server}'
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: lelouchgodmember@gmail.com
    password: jsrezfgunolfrctn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080


